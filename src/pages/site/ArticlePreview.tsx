import { memo } from 'react'

const ArticlePreview = memo(() => {
  return (
    <main className="container mt-14 mb-20">
      <div className='grid xl:grid-cols-24 grid-cols-12 gap-6'>
        
      </div>
    </main>
  )
})

export default ArticlePreview